This algorithm performs LU Decompostion on a given square invertible matrix.
luFactor(A)

LU decomposition with pivoting
 inputs:
	A = coefficient matrix
 outputs:
	L = lower triangular matrix
	U = upper triangular matrix
   P = the permutation matrix
